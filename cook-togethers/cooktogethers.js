console.log("hiiiiiiiiiiiiiiiiiiiii");

const renderUserCard = async (event) => {
  event.preventDefault();
};

const renderNearestUpcomingCard = async (event) => {
  event.preventDefault();
};

const renderUpcomingCards = async (event) => {
  event.preventDefault();
};

const renderReceivedCards = async (event) => {
  event.preventDefault();
};

const renderSentCards = async (event) => {
  event.preventDefault();
};

$(document).ready(() => {
  //renderNearestUpcomingCard();
});
